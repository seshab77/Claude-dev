"""Configuration loading for the Investment Analyzer.

Merges ``.env`` (secrets) and ``config.yaml`` (behavior) into a single
validated :class:`Config` object used throughout the application.

Implemented in Phase 3 scaffolding (stub) / Phase 4 (fully wired).
"""

from __future__ import annotations
